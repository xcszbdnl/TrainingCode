#include <cstdio>
#include <string.h>

const int MAX_NUMBER = 10005;

int vis[MAX_NUMBER];
int depth[MAX_NUMBER];
char input[100];

int main() {
	while (gets(input)) {
		if (input[0] == '\n') {

		}
	}
}