#include <cstdio>
#include <cstring>
#include <set>

using namespace std;

set<double> p;
set<double>::iterator it;s
struct Circle {
    double r, x, y;
};

struct Circle circle[maxn];
int main() {
    freopen("in.txt", "r", stdin);
    while (scanf("%d", &n) != EOF) {
        for (int i = 1; i <= n; i++) {
            scanf("%lf%lf%lf", )
        }
    }
}
