#include <cstdio>
#include <cstring>
#include <algorithm>

using namespace std;
const int maxn = 200007;
int n;
int dp[maxn], len[maxn];
int main() {
  freopen("in.txt", "r", stdin);
  scanf("%d", &n);
  dp[0] = -1;
  for (int i = 1; i <= n; i++) {
    
  }
}
