#include <cstdio>
#include <cstring>
#include <cmath>

const int maxn = 1000007;

struct Query {
    int l, r, order;
    int c;
};

struct Query q[maxn / 10];
int ans[maxn], temp_ans[maxn];
char in[maxn];
int main() {}
